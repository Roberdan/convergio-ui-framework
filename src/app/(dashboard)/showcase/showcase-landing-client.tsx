'use client';

import { ShowcaseSignaturePreview } from './showcase-signature-preview';

/** Landing preview for the framework explorer homepage. */
export function ShowcaseLandingClient() {
  return <ShowcaseSignaturePreview />;
}
