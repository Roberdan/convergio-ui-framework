/**
 * @deprecated — Import { loadPageConfig } from "@/lib/config-loader" instead.
 * Use loadPageConfig("/") for the dashboard page.
 * This re-export exists only for backward compatibility.
 */
export { loadPageConfig as dashboardConfig } from "@/lib/config-loader";
