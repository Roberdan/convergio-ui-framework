/**
 * @deprecated — Import { loadNavSections } from "@/lib/config-loader" instead.
 * This re-export exists only for backward compatibility.
 */
export { loadNavSections as defaultNavSections } from "@/lib/config-loader";
