/**
 * @deprecated — Import { loadAIConfig } from "@/lib/config-loader" instead.
 * This re-export exists only for backward compatibility.
 */
export { loadAIConfig as aiConfig } from "@/lib/config-loader";
